import { User, Recipe } from './model.js';

await db.sync({ force: true})

  await Recipe.create({
    
  });

  await Recipe.create({
    
  });

  await Recipe.create({
    
  });

  await User.create({
    
  });

  await User.create({
    
  });

  await User.create({

  });

  await db.close()